import React from 'react';

const Course = ({ courseData }) => {
    const { courseName, courseRatings, coursePrice, courseImage, details } = courseData
    return (
        <>
         
        </>
    );
};

export default Course;